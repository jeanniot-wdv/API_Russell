<!DOCTYPE html>
<html lang="fr">
<head>
  <%- include('partials/head') %>
  <title>Réservations</title>
</head>
<body>
  <%- include('partials/navbar') %>

  <main>
    <section>
      <h1><i class="fas fa-calendar-check"></i> Gestion des Réservations</h1>
      <p>Modifiez les informations d’une réservation existante :</p>

      <form class="detail-form" action="/api/catways/<%= catway._id %>/bookings/<% booking._id %>?_method=PUT" method="POST">
        <h3><i class="fas fa-pen-to-square"></i> Modifier cette réservation</h3>

        <label for="clientName">Nom du client :</label>
        <input type="text" name="clientName" value="<%= booking.clientName %>" required>

        <label for="boatName">Nom du bateau :</label>
        <input type="text" name="boatName" value="<%= booking.boatName %>" required>

        <label for="startDate">Date de début :</label>
        <input type="date" name="startDate" value="<%= new Date(booking.startDate).toISOString().split('T')[0] %>" required>

        <label for="endDate">Date de fin :</label>
        <input type="date" name="endDate" value="<%= new Date(booking.endDate).toISOString().split('T')[0] %>" required>

        <button type="submit"class="btn-secondary"><i class="fas fa-save"></i> Enregistrer les modifications</button>
      </form>

      <a href="/catways">
        <button class="btn-secondary"><i class="fas fa-arrow-left"></i> Retour à la passerelle</button>
      </a>
    </section>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
