<!DOCTYPE html>
<html lang="fr">
<head>
  <%- include('partials/head') %>
  <link rel="stylesheet" href="/css/style.css">
  <title>Accueil - Catways App</title>
</head>
<body>
  <%- include('partials/navbar', { user }) %>

  <main class="home-container">
    <h1>Bienvenue sur <span class="highlight">Catways App</span></h1>
    <p>Gérez efficacement vos passerelles de port, les réservations de bateaux, et vos utilisateurs via une interface simple et moderne.</p>

    <% if (!user) { %>
      <div class="info-box">
        <h3>Fonctionnalités disponibles après connexion :</h3>
        <ul>
          <li>✔️ Voir et gérer les Catways</li>
          <li>✔️ Créer et suivre les Réservations</li>
          <li>✔️ Gérer les utilisateurs</li>
        </ul>
        <p>Connectez-vous pour accéder au tableau de bord.</p>
      </div>
    <% } else { %>
      <div class="info-box success">
        <p>Vous êtes connecté en tant que <strong><%= user.firstname %> <%= user.name %></strong>. Accédez au <a href="/dashboard">tableau de bord</a>.</p>
      </div>
    <% } %>
    <% if (!user) { %>
  <div class="cta-container">
    <a href="/register" class="btn-primary">Créer un compte utilisateur</a>
  </div>
<% } %>

  </main>

  <%- include('partials/footer') %>
</body>
</html>
